import * as bonjour from 'bonjour';
import * as chai from 'chai';

chai.should();

describe('DeviceMapper', () => {
    describe('#fromService', () => {

        it('should map a service to a device');

        it('should not map a service without addressses');

        it('should not map a service with addressses of wrong type');

        it('should not map a service with empty addressses');

        it('should not map a service without address');

        it('should not map a service without link local address');

        it('should not map a service without MAC address');

        it('should not map a service with MAC address of wrong type');
    });
});
